// Estado padrão do slot paralelo "@modal": nada. Só é substituído quando a
// navegação casa com uma das rotas interceptadas (@modal/(.)novo,
// @modal/(.)[id]/editar).
export default function ModalPadrao() {
  return null;
}
